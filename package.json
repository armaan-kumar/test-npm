{
  "name": "npm-test",
  "description": "Test for npm package using .changeset 3",
  "workspaces": {
    "packages": [
      "packages/**/*"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish-packages": "npm run build && changeset publish"
  },
  "keywords": [],
  "author": "Armaan Kumar",
  "license": "ISC",
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.1",
    "@types/node": "^18.0.6",
    "lerna": "^8.0.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
